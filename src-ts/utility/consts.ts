export const VALID_SCRIPTURE_CODES = ["t"] as const;
export const VALID_VOCALIZATION_KEYS = [
  "usual",
  "simplified",
  "withoutVowel",
] as const;

export const VALID_LANG_CODES = ["en"] as const;

export const DEFAULT_LANG_CODE = "en";

export const OK_RESPONSE_CODE = 200;
